<% content_for :title, "Showing block" %>

<div class="w-full lg:w-2/3 lg:m-auto">
  <% if notice.present? %>
    <p class="py-2 px-3 bg-green-50 mb-5 text-green-500 font-medium rounded-md inline-block" id="notice"><%= notice %></p>
  <% end %>

  <h1 class="font-bold text-4xl">Showing block</h1>

  <%= render @block %>

  <%= link_to "Edit this block", edit_block_path(@block), class: "w-full sm:w-auto text-center rounded-md px-3.5 py-2.5 bg-gray-100 hover:bg-gray-50 inline-block font-medium text-gray-500" %>
  <%= link_to "Back to blocks", blocks_path, class: "w-full sm:w-auto text-center mt-2 sm:mt-0 sm:ml-2 rounded-md px-3.5 py-2.5 bg-gray-100 hover:bg-gray-50 inline-block font-medium text-gray-500" %>
  <%= button_to "Destroy this block", @block, method: :delete, form_class: "sm:inline-block mt-2 sm:mt-0 sm:ml-2", class: "w-full rounded-md px-3.5 py-2.5 text-white bg-brunello-rust font-medium cursor-pointer", data: { turbo_confirm: "Are you sure?" } %>
</div>
